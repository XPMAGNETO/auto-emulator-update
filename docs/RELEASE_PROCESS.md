# Release process

Release branches are temporary triggers used when a direct tag-creation action is unavailable. The release workflow publishes versioned assets and a GitHub prerelease. Normal development remains on `main`.
