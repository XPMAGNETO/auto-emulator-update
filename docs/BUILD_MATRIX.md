CI validates Windows, Ubuntu/Linux, and macOS. Release packaging additionally emits SteamOS-compatible AppImage assets and recognizes Pop!_OS, CachyOS, Batocera, and RetroBat environments at runtime.
